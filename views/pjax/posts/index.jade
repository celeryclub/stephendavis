small.archive-link!= linkTo('Month archive', '/blog/archive')
- for post in posts
  article.post
    h2.post-title
      != linkTo(post.title, "/blog/#{post.slug}")
      //- small.published #{post.published.strftime('%b %e')}
    != post.body
    //- != markdown(post.body)
